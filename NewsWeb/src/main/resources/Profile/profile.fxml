<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane
        prefWidth="420"
        prefHeight="720"
        stylesheets="@css/profile.css"
        xmlns="http://javafx.com/javafx/17"
        xmlns:fx="http://javafx.com/fxml/1"
        fx:controller="org.example.controller.ProfileController">

   <top>
      <HBox styleClass="header">
         <Label text="BÁO MỚI" styleClass="app-title"/>
         <Region HBox.hgrow="ALWAYS"/>
         <Button text="🏠" styleClass="icon-button"/>
      </HBox>
   </top>

   <center>
      <VBox spacing="16">
         <padding>
            <Insets top="20" right="15" bottom="15" left="15"/>
         </padding>

         <VBox
                 fx:id="userInfoBox"
                 spacing="15"
                 styleClass="profile-card"
                 visible="true"
                 managed="true">

            <HBox spacing="15" alignment="CENTER_LEFT">
               <ImageView
                       fx:id="avatarImage"
                       fitWidth="80"
                       fitHeight="80"
                       preserveRatio="true"/>

               <VBox spacing="6">
                  <Label
                          fx:id="usernameLabel"
                          text="Username"
                          styleClass="profile-name"/>

                  <Label
                          fx:id="emailLabel"
                          text="email@gmail.com"
                          styleClass="profile-email"/>

                  <Label
                          fx:id="roleLabel"
                          text="USER"
                          styleClass="role-badge"/>
               </VBox>
            </HBox>
         </VBox>

         <Label
                 fx:id="postTitleLabel"
                 text="Bài viết của bạn"
                 styleClass="section-title"
                 visible="true"
                 managed="true"/>

         <ListView
                 fx:id="userPostsList"
                 styleClass="news-list"
                 visible="true"
                 managed="true"/>
      </VBox>
   </center>

</BorderPane>
