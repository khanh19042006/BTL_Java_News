<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>

<BorderPane prefWidth="420"
            prefHeight="720"
            stylesheets="@css/news-detail.css"
            xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.controller.NewsDetailController">

    <top>
        <HBox spacing="10"
              alignment="CENTER_LEFT"
              styleClass="header">
            <Button text="⬅" fx:id="backBtn"
                    styleClass="icon-button"/>

            <Label text="CHI TIẾT BÀI BÁO"
                   styleClass="app-title"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Button fx:id="editBtn"
                    text="🖊"
                    styleClass="icon-button"/>

            <Button fx:id="saveBtn"
                    text="✓"
                    visible="false"
                    managed="false"
                    styleClass="icon-button"/>

            <Button fx:id="cancelBtn"
                    text="✖"
                    visible="false"
                    managed="false"
                    styleClass="icon-button"/>
        </HBox>
    </top>

    <center>
        <ScrollPane fitToWidth="true"
                    vbarPolicy="AS_NEEDED">
            <content>
                <VBox spacing="16"
                      alignment="TOP_CENTER"
                      styleClass="page-content">
                    <padding>
                        <Insets top="16" right="16" bottom="16" left="16"/>
                    </padding>

                    <VBox spacing="12"
                          styleClass="profile-card">

                        <StackPane>
                            <ImageView fx:id="thumbnailImage"
                                       fitWidth="360"
                                       preserveRatio="true"/>
                            <Button fx:id="editThumbnailBtn"
                                    text="🖊"
                                    visible="false"
                                    managed="false"
                                    onAction="#onEditThumbnail"
                                    StackPane.alignment="BOTTOM_RIGHT"/>
                        </StackPane>

                        <Label fx:id="titleLabel"
                               text="Tiêu đề bài báo"
                               wrapText="true"
                               styleClass="detail-title"/>

                        <HBox spacing="12" styleClass="detail-meta">
                            <Label fx:id="dateLabel" text="24/12/2006"/>
                            <Label text="·"/>
                            <Label fx:id="categoryLabel" text="Công nghệ"/>
                        </HBox>

                        <HBox spacing="10"
                              visible="false"
                              managed="false"
                              fx:id="metaEditBox">

                            <DatePicker fx:id="publishDatePicker"
                                        prefWidth="160"/>

                            <ComboBox fx:id="categoryBox"
                                      prefWidth="160"
                                      promptText="Chọn thể loại"/>
                        </HBox>

                        <Label fx:id="contentLabel"
                               text="Nội dung chi tiết bài báo sẽ hiển thị ở đây..."
                               wrapText="true"
                               styleClass="detail-content"/>


                        <TextField fx:id="titleField"
                                   visible="false"
                                   managed="false"
                                   promptText="Nhập tiêu đề bài báo"
                                   styleClass="edit-title"/>

                        <TextArea fx:id="contentArea"
                                  visible="false"
                                  managed="false"
                                  wrapText="true"
                                  promptText="Nhập nội dung bài báo"
                                  styleClass="edit-content"/>

                    </VBox>
                </VBox>
            </content>
        </ScrollPane>
    </center>

</BorderPane>
